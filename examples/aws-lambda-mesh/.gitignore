# esbuild bundle output (see scripts/bundle.mjs)
artifacts/

# Terraform working files. backend.tf is generated by CI (deploy workflow) for remote state;
# local deploy.sh uses local state, so neither the backend shim nor state should be committed.
deploy/backend.tf
deploy/.terraform/
deploy/.terraform.lock.hcl
deploy/*.tfstate
deploy/*.tfstate.*
