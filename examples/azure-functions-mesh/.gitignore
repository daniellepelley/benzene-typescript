# esbuild bundle output (see scripts/bundle.mjs)
artifacts/

# Terraform working files. backend.tf is generated by CI (deploy workflow) for remote state; local runs
# use local state, so neither the backend shim nor state should be committed.
deploy/backend.tf
deploy/.terraform/
deploy/.terraform.lock.hcl
deploy/*.tfstate
deploy/*.tfstate.*
