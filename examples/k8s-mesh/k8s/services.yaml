# The three Cloud Services: one image (benzene-ts-k8smesh-service) deployed three times, each selecting
# its domain via MESH_SERVICE. Each Kubernetes Service carries the `benzene: "true"` label so the mesh
# discovers it; the mesh's own Service (mesh.yaml) deliberately does not. Matches .NET K8sMesh's
# k8s/services.yaml shape and env var names.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders
  namespace: benzene-ts-mesh
spec:
  replicas: 1
  selector:
    matchLabels: { app: orders }
  template:
    metadata:
      labels: { app: orders }
    spec:
      containers:
        - name: orders
          image: benzene-ts-k8smesh-service:local
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8080 }]
          env:
            - { name: MESH_SERVICE, value: orders }
            # Report to the mesh pod's collector (register/heartbeat/traces) so the Mesh UI's live
            # Fleet plane populates. In-cluster the mesh Service is reachable by name in this namespace.
            - { name: MESH_COLLECTOR_ENVELOPE_URL, value: http://mesh/benzene/invoke }
            # Chain the next hop over HTTP: orders posts payment:take to the payments service's
            # BenzeneMessage endpoint, addressed by in-cluster DNS. Service port is 80, so no :8080 here.
            - { name: DOWNSTREAM_MSG_URL, value: "http://payments/benzene-message" }
          readinessProbe:
            httpGet: { path: /benzene/health, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: benzene-ts-mesh
  labels: { benzene: "true" }
spec:
  selector: { app: orders }
  ports: [{ port: 80, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments
  namespace: benzene-ts-mesh
spec:
  replicas: 1
  selector:
    matchLabels: { app: payments }
  template:
    metadata:
      labels: { app: payments }
    spec:
      containers:
        - name: payments
          image: benzene-ts-k8smesh-service:local
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8080 }]
          env:
            - { name: MESH_SERVICE, value: payments }
            - { name: MESH_COLLECTOR_ENVELOPE_URL, value: http://mesh/benzene/invoke }
            # Second hop: payments posts shipment:book to the shipping service.
            - { name: DOWNSTREAM_MSG_URL, value: "http://shipping/benzene-message" }
          readinessProbe:
            httpGet: { path: /benzene/health, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payments
  namespace: benzene-ts-mesh
  labels: { benzene: "true" }
spec:
  selector: { app: payments }
  ports: [{ port: 80, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping
  namespace: benzene-ts-mesh
spec:
  replicas: 1
  selector:
    matchLabels: { app: shipping }
  template:
    metadata:
      labels: { app: shipping }
    spec:
      containers:
        - name: shipping
          image: benzene-ts-k8smesh-service:local
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8080 }]
          env:
            - { name: MESH_SERVICE, value: shipping }
            - { name: MESH_COLLECTOR_ENVELOPE_URL, value: http://mesh/benzene/invoke }
          readinessProbe:
            httpGet: { path: /benzene/health, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: shipping
  namespace: benzene-ts-mesh
  labels: { benzene: "true" }
spec:
  selector: { app: shipping }
  ports: [{ port: 80, targetPort: 8080 }]
