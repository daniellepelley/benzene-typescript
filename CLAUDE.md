# Benzene TypeScript — Project Guide for Claude Code

See @AGENTS.md for the full project guide and conventions — the same content other AI coding
tools read via the cross-tool `AGENTS.md` standard. This file exists only because Claude Code
specifically looks for `CLAUDE.md`; keep it a thin pointer rather than duplicating content here,
to avoid the two drifting out of sync.
